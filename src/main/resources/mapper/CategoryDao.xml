<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wushengjie.dao.CategoryDao">
    <!--auto generated Code-->
    <resultMap id="AllColumnMap" type="com.wushengjie.vo.Category">
        <result column="NAME" property="name"/>
        <result column="SORT" property="sort"/>
        <result column="ID" property="id"/>
        <result column="CREATE_TIME" property="createTime"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="all_column">
        `NAME`,
        `SORT`,
        `ID`,
        `CREATE_TIME`
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="pojo.id">
        INSERT INTO CATEGORY (
            `NAME`,
            `SORT`,
            `ID`,
            `CREATE_TIME`
        ) VALUES (
            #{pojo.name},
            #{pojo.sort},
            #{pojo.id},
            #{pojo.createTime}
        )
    </insert>

    <!--auto generated Code-->
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="pojo.id">
        INSERT INTO CATEGORY
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="pojo.name!=null"> `NAME`,</if>
            <if test="pojo.sort!=null"> `SORT`,</if>
            <if test="pojo.id!=null"> `ID`,</if>
            <if test="pojo.createTime!=null"> `CREATE_TIME`,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="pojo.name!=null">#{pojo.name},</if>
            <if test="pojo.sort!=null">#{pojo.sort},</if>
            <if test="pojo.id!=null">#{pojo.id},</if>
            <if test="pojo.createTime!=null">#{pojo.createTime},</if>
        </trim>
    </insert>

    <!--auto generated Code-->
    <insert id="insertList">
        INSERT INTO CATEGORY (
        <include refid="all_column"/>
        )VALUES
        <foreach collection="pojos" item="pojo" index="index" separator=",">
            (
            #{pojo.name},
            #{pojo.sort},
            #{pojo.id},
            #{pojo.createTime}
            )
        </foreach>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE CATEGORY
        <set>
            <if test="pojo.name != null"> `NAME` = #{pojo.name}, </if>
            <if test="pojo.sort != null"> `SORT` = #{pojo.sort}, </if>
            <if test="pojo.id != null"> `ID` = #{pojo.id}, </if>
            <if test="pojo.createTime != null"> `CREATE_TIME` = #{pojo.createTime} </if>
        </set>
        WHERE ID = #{pojo.id}
    </update>

<!--auto generated by codehelper on 2017-07-08 12:47:13-->
    <select id="findAll" resultMap="AllColumnMap">
        select
        <include refid="all_column"/>
        from CATEGORY
    </select>

    <select id="initPage" resultType="int">
        select count(id) from CATEGORY where `NAME` like concat('%',#{name},'%')
    </select>

<!--auto generated by codehelper on 2017-07-08 13:28:10-->
    <select id="findByName" resultMap="AllColumnMap">
        select
        <include refid="all_column"/>
        from CATEGORY
        where `NAME` like concat('%',#{name},'%')
        order by SORT
    </select>

<!--auto generated by codehelper on 2017-07-08 16:31:26-->
    <select id="findById" resultMap="AllColumnMap">
        select
        <include refid="all_column"/>
        from CATEGORY
        where `ID`=#{id}
    </select>

</mapper>

